branch: 'master'
schema: 'schema.graphql' # an output of `$ graphql-inspector introspect ...`
icon :  'check-circle'
color : 'purple'
{
  // ...
  "graphql-inspector": {
    "branch": "gcloud",
    "schema": "schema.graphql"
  }
}
